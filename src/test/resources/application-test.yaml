spring:
  datasource:
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        default_schema: mos
  flyway:
    schemas: mos
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true